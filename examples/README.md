# Demo repository

`demo_repository` is a small, self-contained example used to exercise indexing,
search, inspection, controlled patching, verification, and audit output.

After installing the package, from the extraction root:

```console
python examples/quickstart.py
```

The script first copies the fixture to a temporary directory, so the checked-in
example is never modified.
